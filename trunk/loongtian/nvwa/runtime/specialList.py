#!/usr/bin/env python
# -*- coding: utf-8 -*-

__author__ = 'Leon'

"""
[运行时对象]为在mind处理中区隔list使用（无实际意义）
"""

class RelatedRealObjs(list):
    """
    [运行时对象]一个metadata取出的所有relatedReal，为在mind处理中区隔list使用（无实际意义）
    """


class RelatedRealChain(list):
    """
    [运行时对象]多个metadata取出的所有relatedReal，为在mind处理中区隔list使用（无实际意义）
    """


class GroupedReals(list):
    """
    [运行时对象]由一个实际对象列表分组出的所有可能的分组，为在mind处理中区隔list使用（无实际意义）
    """


